import React, { useContext } from 'react'
import { AprendaContext } from '../../../context/AprendaContext'

const HookUseContext = () => {
    const contexto = useContext(AprendaContext);
  return (
    <div>

    </div>
    
  )
}

export default HookUseContext